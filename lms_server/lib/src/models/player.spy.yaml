class: Player
table: player
fields:
  userInfo: module:auth:UserInfo?, relation
  password: String
  totalPoint: int
  quizesDone: int
  sounds: bool
  notification: bool
  rank: Rank?


indexes:
  player_info_id_unique_idx:
    fields: userInfoId
    unique: true